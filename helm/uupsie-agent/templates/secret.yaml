apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/name: uupsie-agent
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
  api-token: {{ required "apiToken is required" .Values.apiToken | b64enc | quote }}
